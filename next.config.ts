import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Allow all dev origins (mobile, LAN, etc.)
  allowedDevOrigins: ['*'],
  // Add other config options as needed
};

export default nextConfig;
